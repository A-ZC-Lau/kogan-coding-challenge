Before running, make sure to install dependencies with:
```javascript
npm install
```

To run, use command:
```javascript
npm start
```

To test, use command:
```javascript
npm test
```
There is only 1 test